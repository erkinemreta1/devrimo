fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
SQLAlchemy>=2.0.36,<3.0
greenlet>=3.1,<4.0
alembic>=1.14,<2.0
asyncpg>=0.30,<1.0
httpx>=0.27,<1.0
PyJWT[crypto]>=2.9,<3.0
cryptography>=43.0,<44.0
structlog>=24.4,<25.0
# Agent framework. `mcp` is pinned below 2.0 deliberately: 2.x renamed
# McpError -> MCPError and agno 3.x still imports the old name, so an
# unpinned install fails at import with a misleading "`mcp` not installed".
agno==3.0.1
mcp==1.29.1
opentelemetry-sdk>=1.28,<2.0
openinference-instrumentation-agno>=0.1,<1.0
openai>=1.60,<4.0
# Agno's database layer is synchronous and builds its own engine, so Postgres
# deployments need the sync driver alongside asyncpg.
psycopg[binary]>=3.2,<4.0
pgvector>=0.3,<1.0
pypdf>=5.1,<6.0
beautifulsoup4>=4.12,<5.0
feedparser>=6.0.12,<7.0
icalendar>=6.3,<7.0
simpleeval>=1.0.3,<2.0
# Server-only Auth Admin client. Keep this explicit rather than depending on
# the old gotrue package names that supabase-py no longer exposes directly.
supabase>=2.18.1,<3.0
# Observability. `posthog` arrives transitively via agno, but every LLM
# generation, exception, and log line in this service depends on it, so it is
# pinned here rather than left to a transitive resolution. The `otel` extra
# brings the OTLP HTTP exporter used to ship structlog output to PostHog Logs.
posthog[otel]>=7.44,<8.0
opentelemetry-exporter-otlp-proto-http>=1.28,<2.0
